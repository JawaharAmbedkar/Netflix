import Link from 'next/link';
import type { Media } from '../lib/tmdb';
export function MediaRow({ title, media }: { title: string; media: Media[] }) { return <section className="mb-10"><h2 className="mb-4 text-2xl font-bold">{title}</h2><div className="grid grid-cols-2 gap-4 sm:grid-cols-3 md:grid-cols-5 lg:grid-cols-6">{media.map((item) => { const href = item.media_type === 'movie' ? `/watch/movie/${item.id}` : `/watch/tv/${item.id}/1/1`; return <Link key={`${item.media_type}-${item.id}`} href={href} className="group"><div className="aspect-[2/3] overflow-hidden rounded-md bg-zinc-800">{item.poster_path ? <img src={item.poster_path} alt={item.title} className="h-full w-full object-cover transition group-hover:scale-105" /> : null}</div><p className="mt-2 truncate font-medium">{item.title}</p><p className="text-sm text-zinc-400">{item.release_date.slice(0, 4) || '—'} · ★ {item.rating.toFixed(1)}</p></Link>; })}</div></section>; }
